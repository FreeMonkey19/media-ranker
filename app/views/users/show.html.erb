
<main>
<h2 class="user">User Summary: <%= @user.name %></h2>
<%# %b is the abbreviated month name ie:Jan %>
<p>Joined site <%= (@user.created_at).strftime('%b %m, %Y')%></p>
<section class="user-votes__container">
<h3 class="user-votes__header">Votes</h3>

<table class="table">
  <thead>
    <tr>
      <th>Media Title</th>
      <th>Created By</th>
      <th>Published</th>
      <th>Category</th>
      <th>Voted On</th>
    </tr>
  </thead>
  <tbody>
      <% @user.votes.each do |vote| %>
      <tr>
        <td><%= link_to(vote.work.title, work_path(vote.work.id), alt: "link to work show page") %></td>
        <td><%= vote.work.creator %></td>
        <td><%= vote.work.publication_year %></td>
        <td><%= vote.work.category %></td>
        <td><%= (vote.created_at).strftime('%b %m, %Y') %></td>
        <% end %>
      </tr>
  </tbody>
</table>
</section>


  <%= link_to("See all Users", users_path, method: :get, alt: "link to all users", class: "btn btn-secondary") %>

  <%= link_to("Back to Media List", root_path, method: :get, alt: "link to main page", class: "btn btn-primary") %>
</main>